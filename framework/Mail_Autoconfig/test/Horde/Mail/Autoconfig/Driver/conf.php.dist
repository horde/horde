<?php
$conf['mail_autoconfig'] = array(
    /* Test domains used for the autoconfig driver tests. */
    'domains' => array(
        /* As of May 2014, Gmail supports SRV records. */
        array(array('gmail.com')),
        array(array('test.mail.gmail.com', 'mail.gmail.com', 'gmail.com'))
    )
);
