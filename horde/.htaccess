allow from all

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond   %{REQUEST_FILENAME}  !-d
    RewriteCond   %{REQUEST_FILENAME}  !-f
    RewriteRule ^(.*)$ rampage.php [QSA,L]
</IfModule>

php_value include_path @php_dir@
SetEnv PHP_PEAR_SYSCONF_DIR @horde_dir@
