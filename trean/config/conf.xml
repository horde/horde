<?xml version="1.0"?>
<!-- $Id$ -->
<configuration>
 <configsection name="storage">
  <configheader>Storage System Settings</configheader>

  <configswitch name="driver" desc="What storage driver should we use?">sql
   <case name="sql" desc="SQL">
    <configsection name="params">
     <configsql switchname="driverconfig"/>
    </configsection>
   </case>
  </configswitch>
 </configsection>

 <configsection name="content_index">
  <configheader>Content Indexing/Search</configheader>
  <configdescription>Search support requires the Content_Index application,
  which currently assumes an ElasticSearch cluster accessible by Horde at the
  default port of localhost:9200. If you don&apos;t have that, don&apos;t turn
  content indexing/search on.</configdescription>
  <configboolean name="enabled" desc="Enable use of Content_Index?">false</configboolean>
 </configsection>

 <configsection name="favicons">
  <configheader>Virtual File Storage</configheader>
  <configvfs switchname="type" />
 </configsection>
</configuration>
